import sockets
import ssl

class run_pack:
    def __init__(self, dest_host, dest_port, run_command, use_tls):
        self.dest_host = dest_host
        self.dest_port = dest_port
        self.run_command = run_command
        self.use_tls = use_tls
        
        
    if __name__ == "__main__":
    main()